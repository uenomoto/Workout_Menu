<div class="container">
  <div class="row m-5 text-center">
    <div class="col-md-12">
      <h1><%= @menu.day %>日の<%= @menu.training_name.name %>詳細</h1>
    </div>
  </div>
  <div class="row m-3">
    <div class="col-md-12">
      <table class="table table-bordered table-dark">
          <tr>
            <th>日付</th>
            <td><%= @menu.day %></td>
          </tr>
          <tr>
            <th>名前</th>
            <td><%= @menu.training_name.name %></td>
          </tr>
            <tr>
              <th>部位</th>
              <td><%= @menu.training_name.genre.name %></td>
            </tr>
            <tr>
              <th>レップ×セット</th>
              <td><%= @menu.count %>×<%= @menu.set %></td>
            </tr>
            <tr>
              <th>重さ</th>
              <td>
              <% if @menu.status == true %>
              <p><%= @menu.weight %>kg</p>
              <% else %>
              <p>自重</p>
              <% end %>
              </td>
           </tr>
      </table>
    </div>
  </div>
  <div class="row m-5 text-center">
    <% if @menu.user_id == current_user.id %>
    <div class="col-md-4">
      <%= link_to "メニュー編集", edit_training_menu_path(@menu.id), class: "btn btn-success" %>
    </div>
    <div class="col-md-4"></div>
    <div class="col-md-4">
      <%= link_to "メニュー削除", training_menu_path(@menu.id), method: :delete, "data-confirm" => "削除してもよろしいですか？", class: "btn btn-danger" %>
    </div>
    <% end %>
  </div>
</div>
